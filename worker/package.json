{
  "name": "avel-diagnostico-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Backend serverless (Cloudflare Worker) que chama a API da Claude para gerar o Diagnóstico Financeiro Inteligente da Ável Planejamento.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:set-key": "wrangler secret put ANTHROPIC_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "zod": "^3.23.8"
  }
}
